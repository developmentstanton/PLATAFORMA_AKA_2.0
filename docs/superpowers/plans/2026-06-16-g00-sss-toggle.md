# G00 — Toggle del control S.S.S — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans / subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** El control S.S.S de Ventas (G00) arranca sin selección y cada botón se prende/apaga con clic, excluyentes.

**Architecture:** Frontend puro. Quitar el `active` por defecto de "No Same"; añadir un manejador toggle dedicado para `#g00-sss` (dejando `initSeg` radio para `#g00-cal`). `buildParams` y backend sin cambios (ninguno → `nosame` → todas las tiendas).

**Tech Stack:** PHP/HTML/JS vanilla (`informes/g00.php`).

**Spec:** `docs/superpowers/specs/2026-06-16-g00-sss-toggle-design.md`

---

## Task 1: Toggle excluyente del control S.S.S

**Files:**
- Modify: `informes/g00.php` (HTML del botón "No Same"; nueva función JS `initSegToggle`; reemplazo de `initSeg('g00-sss')`).

- [ ] **Step 1: HTML — quitar `active` por defecto de "No Same"**

Reemplazar (~línea 204):
```html
                    <button type="button" class="g00-seg-btn active" data-val="nosame">No Same</button>
```
por:
```html
                    <button type="button" class="g00-seg-btn" data-val="nosame">No Same</button>
```

- [ ] **Step 2: JS — añadir `initSegToggle`**

Inmediatamente DESPUÉS de la función `initSeg` (que termina en `}` tras su `forEach`, ~línea 379), añadir:
```javascript
    // Variante toggle para S.S.S: se puede dejar ninguno activo; clic en el activo lo apaga;
    // los botones del grupo son mutuamente excluyentes.
    function initSegToggle(id) {
        document.querySelectorAll('#' + id + ' .g00-seg-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                const wasActive = btn.classList.contains('active');
                document.querySelectorAll('#' + id + ' .g00-seg-btn').forEach(b => b.classList.remove('active'));
                if (!wasActive) btn.classList.add('active');
                g00Load();
            });
        });
    }
```

- [ ] **Step 3: JS — usar `initSegToggle` para `#g00-sss`**

Reemplazar la línea:
```javascript
        initSeg('g00-sss');
```
por:
```javascript
        initSegToggle('g00-sss');
```
(Dejar `initSeg('g00-cal');` intacto.)

- [ ] **Step 4: Lint**

Run: `php -l informes/g00.php`
Expected: `No syntax errors detected`.

- [ ] **Step 5: Commit**

```bash
git add informes/g00.php
git commit -m "feat(g00): control S.S.S como toggle excluyente (default sin seleccion)"
```
(Terminar con el trailer Co-Authored-By.)

---

## Task 2: Handoff E2E

- [ ] **Step 1: Verificación navegador (Rafael).** NO ejecutar navegador aquí.

Checklist (REPORTES → Ventas):
1. Al entrar, **ningún** botón S.S.S resaltado; el informe carga (todas las tiendas).
2. Clic en **Same** → se resalta, "No Same" apagado; datos = same-store (tiendas cerradas en el rango desaparecen).
3. Clic de nuevo en **Same** → se apaga (ninguno); vuelve a todas las tiendas.
4. Clic en **No Same** → se resalta solo él; datos = todas. Clic otra vez → se apaga.
5. Nunca quedan los dos encendidos.
6. **Calendario** (Día a Día / Retail) sigue con uno siempre activo.

---

## Self-Review

- Default sin selección → Step 1 (quita `active`). ✓
- Toggle on/off + excluyente → Step 2 (`initSegToggle`: `wasActive` apaga; si no, limpia todos y activa uno). ✓
- Calendario intacto → Step 3 (solo cambia `g00-sss`; `g00-cal` sigue con `initSeg`). ✓
- Ninguno = todas → sin tocar `buildParams` (`segValue||'nosame'`). ✓
- Sin placeholders; código literal. ✓
- Nombres: `initSegToggle` nuevo, `initSeg`/`segValue`/`g00Load` existentes reutilizados. ✓
